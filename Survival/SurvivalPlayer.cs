using System;

public class SurvivalPlayer
{
    public SurvivalPlayer(string uname) { username = uname; }

    public float currentTime;
    public string username;
    public int points = 0;
    public int lives = 1;
}